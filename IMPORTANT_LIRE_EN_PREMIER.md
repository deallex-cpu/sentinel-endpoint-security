# ⚠️ IMPORTANT — LIRE AVANT D'INSTALLER

## Le fichier `server.js` sur ce repo est un PLACEHOLDER

Le vrai fichier `server.js` (927 lignes, 10 modules) était trop gros
pour être pushé via l'API GitHub automatiquement.

### 📥 Comment récupérer le VRAI server.js :

**Le fichier complet t'a été donné directement dans la session Claude.**
Regarde dans les fichiers de sortie de la session — tu y trouveras:
- `server.js` (927 lignes, ~58 KB)
- `index.html` (design Palo Alto, ~37 KB)

### 📝 Ce que tu dois faire :

1. **Télécharger** `server.js` et `index.html` depuis les outputs de la session Claude
2. **Copier** ces 2 fichiers dans ton dossier `dashboard/`
3. Ils remplacent les fichiers existants
4. **Lancer** : `node server.js` ou double-clic sur `LANCER_DASHBOARD.bat`

### Vérification rapide :
Si ton `server.js` fait 927 lignes et commence par:
```
// SENTINEL v3.0 — Endpoint Security Platform
// 10 modules: Auto-Response, IOC Scanner, FIM, Canary, Hardening...
```
→ C'est le bon fichier.

Si ton `server.js` fait moins de 50 lignes → c'est le placeholder, remplace-le.

---

Pour le guide complet d'installation, voir [INSTALL.md](INSTALL.md)
